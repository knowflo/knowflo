There's a new question in the <%= Settings.app_name %> <%= @question.group.name %> group.

<%= @question.user.name %> asked "<%= @question.subject %>"

"<%= @question.body %>"

You can get involved in the conversation by visiting <%= question_url(@question, subdomain: @question.group) %>.
If you'd prefer to stop receiving emails like this, change your notification settings at <%= settings_url %>.
